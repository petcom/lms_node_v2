# Mock Data Use Report (Legacy)

This document is kept for historical context. For the current mock database setup
and credentials, see:

- `scripts/README.md`
- `scripts/MockData_Use_Report.md`

The mock database is now `lms_mock` and uses `.env.mock` via `ENV_FILE`.
